本目录可预留放置 **运行时加载** 的字体或说明（例如若日后启用 `LV_USE_TINY_TTF` 等）。

当前中文界面使用工程内 **`src/noto_sans_sc_16.c`** 静态位图字库，**不依赖**本目录下的文件。

更换或扩展中文字形：请使用 LVGL 字体转换器重新生成 `.c` 并替换 `src/noto_sans_sc_16.c`，详见 `.cursor/rules.md`「二、静态中文字库」。
